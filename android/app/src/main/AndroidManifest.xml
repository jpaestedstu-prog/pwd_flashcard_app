<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- TV Cast: the app runs a local HTTP server so TVs on the same
         Wi-Fi can poll it. INTERNET is required even for listening
         sockets on most Android OEM builds. ACCESS_WIFI_STATE lets
         us pick the right local IPv4 to show in the cast URL. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Word Hunt: live camera object recognition. required="false" so
         tablets without a camera can still install the app — the scan
         screen shows a friendly fallback instead. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- Voice input: pronunciation games + Gaze Control voice commands
         (speech_to_text). Declared explicitly; the microphone isn't required
         so a device without one still installs. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <!-- Explicit tablet support declaration. Defaults already cover
         these on modern Android, but declaring them suppresses Play
         Store warnings about "designed for phones" and makes the
         intent visible to anyone auditing the manifest. -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:resizeable="true"
        android:anyDensity="true"/>

    <application
        android:label="FlashLearn PWD"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Portrait-only on large screens too. Android 16 (API 36) ignores an
             app's orientation/resizability request on displays ≥ sw600dp by
             default, which would let this app rotate to landscape on tablets
             despite the `screenOrientation="portrait"` lock below. This
             property opts back into compatibility mode so the portrait lock is
             honored (the app is letterboxed on a landscape-held tablet instead
             of stretching to landscape) — required for the fixed-portrait
             spatial layout the Gaze Control D-pad depends on. The opt-out is
             available through targetSdk 36; API 37 removes it, at which point
             the app must adopt an adaptive layout. Unknown to Android ≤ 15,
             which safely ignores the element. -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
            android:value="true" />

        <!-- Boot receiver to reschedule notifications after reboot -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Enable Impeller rendering engine for lower latency and
             more consistent frame pacing on Vulkan-capable GPUs. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Voice commands (speech_to_text): on Android 11+ (the app targets
             SDK 36) package-visibility filtering hides the on-device speech
             recognition service unless it's declared here. Without this,
             SpeechToText.initialize() returns false and every spoken command
             (Gaze Control voice, pronunciation games) silently does nothing. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
